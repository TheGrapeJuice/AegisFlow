---
phase: 01-frontend-shell
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified: []
autonomous: false
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - MAP-01
  - MAP-02
  - MAP-03

must_haves:
  truths:
    - "All 5 Phase 1 success criteria from the roadmap are visually confirmed by the user"
    - "No functional blockers exist before moving to Phase 2"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 1 frontend shell — confirming all 5 roadmap success criteria before Phase 2 begins.

Purpose: Phase 1 is a visual milestone. A human must confirm the dashboard looks right before wiring in a real backend. No automated test catches "the map renders correctly" or "the D3 overlay doesn't drift."
Output: User sign-off that Phase 1 is complete, or a list of issues to address before Phase 2.
</objective>

<execution_context>
@C:/Users/saads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/saads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of Phase 1 frontend shell</name>
  <files>none — verification only, no files modified</files>
  <action>
    Present the verification checklist to the user and pause for confirmation.
    Run `npm run dev` to ensure the dev server is running before asking the user to verify.
  </action>
  <what-built>
    Plans 01-03 built the complete Phase 1 frontend shell:
    - Dark-themed Vite + React + TailwindCSS dashboard with sidebar, header, status panel
    - MapLibre GL JS dark map (CARTO Dark Matter) with 24 hardcoded power nodes as colored circles and connecting edge lines
    - D3 SVG overlay with node labels and selection ring synchronized on map pan/zoom
    - Node click detail panel showing name, type, voltage, frequency, and load
    - Map legend explaining status colors (green/yellow/red) and node types
  </what-built>
  <how-to-verify>
    Open http://localhost:5173 in a browser at 1280px+ width.

    Check each success criterion from the Phase 1 roadmap:

    1. **Layout**: Opening the app shows a dark-themed dashboard with sidebar (left), header (top), map canvas (center), status panel (right) — no white/blank areas anywhere

    2. **Map**: The MapLibre map renders a dark-styled city grid with power nodes visible as colored dots (green, yellow, red) and distribution lines connecting them

    3. **D3 overlay sync**: Pan the map left/right and zoom in/out — the node labels and selection ring stay correctly positioned above their nodes with no drift

    4. **Node click**: Click any node dot on the map — the status panel on the right updates to show that node's name, type (substation/generator/etc.), and sensor readings (voltage in kV, frequency in Hz, load %)

    5. **Legend**: A legend is visible on the map canvas (bottom-left) explaining: green = normal, yellow = warning, red = critical, plus node type sizes

    Also verify:
    - No console errors in browser DevTools
    - `npm run build` exits 0 (run in terminal)
  </how-to-verify>
  <verify>
    <automated>npm run build 2>&amp;&amp; echo "BUILD_OK"</automated>
    <manual>All 5 Phase 1 success criteria visually confirmed by user at http://localhost:5173</manual>
  </verify>
  <done>User types "approved" — all 5 roadmap success criteria confirmed, no blocking issues.</done>
  <resume-signal>
    Type "approved" if all 5 criteria pass.
    Or describe any issues (e.g., "D3 labels drift on zoom", "legend not visible", "node click doesn't update panel") and Claude will fix them before Phase 2 begins.
  </resume-signal>
</task>

</tasks>

<verification>
User confirms all 5 Phase 1 roadmap success criteria pass.
</verification>

<success_criteria>
- User types "approved" or equivalent confirmation
- All 5 Phase 1 roadmap success criteria visually verified
- No blocking issues — ready to proceed to Phase 2
</success_criteria>

<output>
After user approves, create `.planning/phases/01-frontend-shell/01-04-SUMMARY.md` with:
- Verification outcome (approved / issues found and resolved)
- Any fixes applied during verification
- Phase 1 completion statement
- Confirmation that all 7 requirement IDs (SHELL-01 through MAP-03) are satisfied
</output>
