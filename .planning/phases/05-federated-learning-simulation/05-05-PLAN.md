---
phase: 05-federated-learning-simulation
plan: 05
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-04"
files_modified: []
autonomous: false
requirements:
  - FL-01
  - FL-02
  - FL-03
must_haves:
  truths:
    - "FL panel shows 3 distinct per-client accuracy curves that visibly diverge (not identical lines)"
    - "Global model accuracy curve shows upward trend over 10 rounds"
    - "After 60-second reload interval, backend log confirms FL model reloaded"
  artifacts: []
  key_links:
    - from: "FLPanel browser render"
      to: "GET /api/fl/status"
      via: "5-second polling visible in browser Network tab"
---

<objective>
Human verification of all 3 Phase 5 success criteria.

Purpose: Confirms the FL simulation is visually compelling and technically correct before Phase 6 deployment. Verifies non-IID training (diverging curves), model improvement (upward global trend), and 60s reload (FL-03).

Output: Human sign-off on FL-01, FL-02, FL-03.
</objective>

<execution_context>
@C:/Users/saads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/saads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 5 FL simulation end-to-end</name>
  <files></files>
  <action>No automated action — this is a human verification checkpoint.</action>
  <what-built>
Phase 5 complete implementation:
- backend/ml/fl_train.py: 10-round FedAvg with 3 geographically partitioned clients (non-IID)
- backend/ml/fl_inference.py: hot-reload inference for 60s model updates
- backend/routers/ws.py: _fl_loop (ThreadPoolExecutor), _fl_reload_loop (60s), /api/fl/status endpoint
- backend/main.py: 5 background tasks (broadcast, inference, cascade, fl, fl-reload)
- src/components/layout/FLPanel.tsx: D3 multi-line accuracy chart, 5s polling
- src/components/layout/StatusPanel.tsx: FLPanel rendered below CascadePanel
  </what-built>
  <how-to-verify>
Start backend and frontend:

Backend terminal:
```
cd "C:/Users/saads/Documents/Aegis Flow"
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000
```

Frontend terminal:
```
cd "C:/Users/saads/Documents/Aegis Flow"
npm run dev
```

Open http://localhost:5173 and verify each criterion:

FL-01 — Non-IID diverging client curves:
- Wait 2-3 minutes for FL simulation to complete (10 rounds)
- Right sidebar FLPanel shows 3 colored lines: blue (North), yellow (Central), green (South)
- The 3 lines are NOT identical — they diverge visibly
- North (blue) accuracy is noticeably higher than South (green) throughout training
- PASS = visible divergence between clients

FL-02 — Dashboard FL panel with training progress:
- During training: FLPanel header shows "Training: Round X / 10" that increments
- After training: shows "Training Complete"
- D3 chart has 4 lines: 3 colored clients + 1 white global (thicker)
- Latest global accuracy % shown below chart in monospace font
- PASS = chart visible with labeled legend and updating round counter

FL-03 — Inference uses FL weights after 60s reload:
- Check backend terminal for "[fl] reloaded global FL model" log line (appears 60s after startup)
- Open browser DevTools Network tab — confirm /api/fl/status requests every ~5 seconds
- WebSocket /ws/nodes still streaming (node colors updating — no freeze during FL reload)
- PASS = "[fl] reloaded" in backend log AND dashboard remains live

Backend log sequence (expected):
1. "Application startup complete"
2. "[fl] round 1/10 global=0.XXXX" through "[fl] round 10/10 global=0.XXXX"
3. "[fl] simulation complete"
4. "[fl] reloaded global FL model" (after 60s)
  </how-to-verify>
  <verify>Human visual inspection per steps above</verify>
  <done>All 3 FL criteria confirmed: diverging client curves (FL-01), live training panel with chart (FL-02), reload log entry and live WebSocket (FL-03).</done>
  <resume-signal>Type "approved" if all 3 criteria pass, or describe which criterion failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 3 Phase 5 success criteria are met.
</verification>

<success_criteria>
- FL-01: 3 client accuracy curves visibly diverge (non-IID proof)
- FL-02: FLPanel shows training progress, D3 4-line chart, global accuracy %
- FL-03: "[fl] reloaded global FL model" in backend log; WebSocket unaffected
</success_criteria>

<output>
After approval, create `.planning/phases/05-federated-learning-simulation/05-05-SUMMARY.md`
</output>
