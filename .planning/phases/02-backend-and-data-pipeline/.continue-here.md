---
phase: 02-backend-and-data-pipeline
task: visual-enhancements
status: in_progress
last_updated: 2026-02-24T22:38:05.590Z
---

<current_state>
Phase 2 plans (02-01 through 02-05) are ALL complete and committed. Phase 2 human verification (02-06) was in progress — user tested the app and found bugs + requested visual improvements BEFORE approving. Two backend bugs were fixed. Visual enhancements were designed via brainstorming and a full implementation plan was written to docs/plans/2026-02-24-visual-enhancements.md. Implementation has NOT started yet — context limit hit before dispatching subagents.
</current_state>

<completed_work>

## Phase 2 Plans (all done):
- 02-01: FastAPI backend — REST topology, WebSocket broadcast, POST /api/storm ✓
- 02-02: InfluxDB client + synthetic data generator ✓
- 02-03: Frontend hooks — useTopology, useNodeWebSocket, DashboardLayout wired ✓
- 02-04: Live map colors + Storm Event sidebar button ✓
- 02-05: NodeCharts D3 sparklines + useNodeHistory hook ✓

## Bug fixes (committed as f4f9067):
- `backend/routers/ws.py`: Fixed `_clients -= dead` → `_clients.difference_update(dead)` (UnboundLocalError)
- `backend/routers/topology.py`: Wrapped `query_node_history` in try/except, returns [] when InfluxDB down (was 500)

## Visual enhancements plan written:
- docs/plans/2026-02-24-visual-enhancements.md (committed as 17f1da1)
- 5 tasks designed and planned, NO code written yet

</completed_work>

<remaining_work>

## Visual Enhancements — 5 tasks, NONE started:

**Task 1: Node Glow — D3Overlay animated halos**
- File: src/components/map/D3Overlay.tsx
- Add STATUS_GLOW config, BASE_RADIUS config
- Refactor useEffect to use layered SVG groups (glow-layer, label-layer, selection-layer, storm-layer)
- Add nodesRef/stormActiveRef/epicenterIdRef refs synced via useEffect
- Add d3.timer() loop that updates glow circle r and fill-opacity via sine wave

**Task 2: StormCanvas — canvas rain particle system**
- File: src/components/map/StormCanvas.tsx (NEW)
- 150 rain particles, diagonal fall (WIND_ANGLE = 0.35 radians), requestAnimationFrame loop
- Only renders/animates when stormActive=true
- Returns null when inactive

**Task 3: Storm D3 SVG elements — shockwave, wind arrows, fog**
- File: src/components/map/D3Overlay.tsx
- SVG defs: feGaussianBlur filter for fog
- Fog ellipse centered on epicenter
- 8 wind arrows with staggered opacity pulse
- Expanding shockwave ring (d3 transition, repeats every 2.3s while storm active)
- All driven by stormTimer (d3.timer) reading from refs

**Task 4: Storm state + toggle in DashboardLayout and Sidebar**
- Files: DashboardLayout.tsx, Sidebar.tsx, GridMap.tsx
- Add stormActive/epicenterId/affectedNodeIds state to DashboardLayout
- handleStormEvent becomes toggle: if active → clear state; if inactive → POST /api/storm, parse response
- Sidebar: add stormActive prop, show "⚡ Stop Storm" (red) when active
- GridMap: add storm props to interface, pass to D3Overlay and StormCanvas

**Task 5: Node blue selection + deselect on background click**
- File: src/components/map/GridMap.tsx
- Add useEffect watching selectedNodeId: call setPaintProperty with 'case' expression for blue circle
- Add map.on('click') in load callback: queryRenderedFeatures — if no node hit, call onNodeClick(null)
- Change onNodeClick type from (GridNode) to (GridNode | null)

## Phase 2 verification (02-06):
- After visual enhancements are implemented, user needs to re-verify all 5 criteria
- Then complete the phase with gsd-tools commit and roadmap update

</remaining_work>

<decisions_made>

- Visual enhancements: Canvas (StormCanvas) for rain particles, D3 SVG for glow/shockwave/wind/fog — best performance split
- Storm toggle: client-side toggle off (no second POST), server call only on activation
- Node glow: always on for ALL nodes (not just warning/critical), status determines speed/intensity
- Storm duration: toggle (stays until user clicks "Stop Storm"), not auto-expire
- Storm visuals: "full weather" style — rain + fog + shockwave + wind arrows
- D3Overlay architecture: refs (nodesRef, stormActiveRef, epicenterIdRef) synced via separate useEffects, main animation useEffect has [map, selectedNodeId] deps only
- Node selection: blue MapLibre circle-color via setPaintProperty + existing D3 dashed ring stays

</decisions_made>

<blockers>

- Phase 2 02-06 (human verification) is INCOMPLETE — user has NOT typed "approved" yet
- Visual enhancements must be implemented first, THEN user re-verifies Phase 2
- InfluxDB is NOT running on user's machine — charts show "Awaiting data..." which is acceptable behavior (graceful degradation implemented)

</blockers>

<context>
We're in an interesting state: Phase 2 execution is 5/6 plans complete, but 02-06 (human verification) is blocked pending visual enhancements. The user found the app "seems to work" but wants better visuals before formally approving. The design is fully approved via brainstorming. The implementation plan is complete and detailed in docs/plans/2026-02-24-visual-enhancements.md.

The approach (subagent-driven development): dispatch one implementer subagent per task sequentially, with spec reviewer + code quality reviewer after each. Tasks must be done IN ORDER (1→2→3→4→5) because tasks 1 and 3 both modify D3Overlay.tsx, and task 4 modifies GridMap.tsx which task 5 also touches.

After all 5 tasks complete, run npm run build to verify, then have user test visually, then complete 02-06 and mark Phase 2 done.
</context>

<next_action>
1. `/clear` to get fresh context
2. `/gsd:resume-work` to load this handoff
3. Then implement visual enhancements using subagent-driven development:
   - Read docs/plans/2026-02-24-visual-enhancements.md
   - Dispatch implementer subagent for Task 1 (D3Overlay node glow)
   - Review → Task 2 (StormCanvas) → Review → Task 3 (storm SVG) → Review → Task 4 (state/toggle) → Review → Task 5 (selection) → Review
   - Run final npm run build
   - Ask user to re-test and type "approved" for Phase 2 02-06
</next_action>
