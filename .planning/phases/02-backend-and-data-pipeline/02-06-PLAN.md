---
phase: 02-backend-and-data-pipeline
plan: 06
type: execute
wave: 4
depends_on: [02-04, 02-05]
files_modified: []
autonomous: false
requirements: [BACK-01, BACK-02, BACK-03, BACK-04, UX-01, UX-02]

must_haves:
  truths:
    - "Map populates from backend REST data on page load (not hardcoded values)"
    - "Node colors update in real time via WebSocket without page refresh"
    - "Selecting a node shows live voltage and frequency charts in sidebar"
    - "Clicking 'Simulate Storm Event' visibly changes node colors within a few seconds"
    - "Disconnecting the WebSocket triggers automatic reconnect (exponential backoff visible in console)"
  artifacts:
    - path: "src/hooks/useTopology.ts"
      provides: "REST fetch on mount"
      exports: ["useTopology"]
    - path: "src/hooks/useNodeWebSocket.ts"
      provides: "WebSocket with exponential backoff"
      exports: ["useNodeWebSocket"]
    - path: "src/components/sidebar/NodeCharts.tsx"
      provides: "D3 sparklines for voltage and frequency"
      exports: ["NodeCharts"]
    - path: "backend/main.py"
      provides: "FastAPI server"
    - path: "backend/state.py"
      provides: "Shared node state + sensor tick simulation"
  key_links:
    - from: "src/components/layout/DashboardLayout.tsx"
      to: "backend/main.py"
      via: "REST fetch for topology + WebSocket for live updates"
      pattern: "useTopology|useNodeWebSocket"
    - from: "src/components/layout/Sidebar.tsx"
      to: "backend/routers/ws.py"
      via: "POST /api/storm triggers state mutation broadcast via WebSocket"
      pattern: "onStormEvent|api/storm"
---

<objective>
Human verification of all 5 Phase 2 success criteria. Claude runs the backend and frontend, walks through each criterion systematically, and the user confirms each one passes before Phase 2 is marked complete.

Purpose: Phase 2 is a major integration milestone — static shell becomes live display. Human sign-off is required before Phase 3 (XGBoost) begins layering ML on top of this infrastructure.

Output: Phase 2 marked complete in ROADMAP.md and STATE.md; SUMMARY.md written.
</objective>

<execution_context>
@C:/Users/saads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/saads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification automated checks</name>
  <files></files>
  <action>
Run the following automated checks before presenting to the user for visual verification:

1. `npm run build` — must exit 0 (TypeScript compiles clean)
2. Python syntax check on all backend files:
   `python -c "import ast; [ast.parse(open(f).read()) for f in ['backend/main.py','backend/models.py','backend/state.py','backend/routers/topology.py','backend/routers/ws.py','backend/influx_client.py','backend/generator.py']]; print('ALL_BACKEND_SYNTAX_OK')"`
3. Confirm no GRID_TOPOLOGY import in GridMap.tsx:
   `grep -n "GRID_TOPOLOGY" src/components/map/GridMap.tsx` → must return no matches
4. Confirm WebSocket backoff logic is present:
   `grep -n "backoffRef\|MAX_BACKOFF" src/hooks/useNodeWebSocket.ts` → must return matches
5. Confirm storm endpoint is wired:
   `grep -n "api/storm" src/components/layout/DashboardLayout.tsx` → must return matches

Print a summary of pass/fail for each check. If any check fails, fix it before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd "C:/Users/saads/Documents/Aegis Flow" && npm run build 2>&1 | grep -E "error|warning|built" | head -10 && echo "BUILD_CHECK_DONE"</automated>
  </verify>
  <done>All 5 automated checks pass; npm run build exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all 5 Phase 2 success criteria</name>
  <action>Human verifies all 5 Phase 2 success criteria interactively in the browser with backend running.</action>
  <verify>
    <manual>All 5 roadmap success criteria confirmed by human reviewer</manual>
  </verify>
  <done>Human types "approved" confirming all 5 Phase 2 success criteria pass</done>
  <what-built>
    Phase 2 complete implementation:
    - FastAPI backend serving topology REST + WebSocket broadcast + storm injection
    - InfluxDB synthetic data generator (5s write interval)
    - Frontend useTopology hook fetching from REST on load
    - Frontend useNodeWebSocket with exponential backoff reconnect
    - Live map node color updates from WebSocket (no page refresh)
    - Storm Event button in sidebar calling POST /api/storm
    - Voltage + frequency sparkline charts in sidebar node detail
  </what-built>
  <how-to-verify>
    Start the backend first:
    ```
    pip install -r backend/requirements.txt
    python -m backend.main
    ```
    Then in a second terminal, start the frontend:
    ```
    npm run dev
    ```
    Open http://localhost:5173 and verify each criterion:

    **Criterion 1 — Topology from backend:**
    - Open Network tab in browser DevTools
    - Reload page
    - Confirm a GET request to http://localhost:8000/api/topology returns 200 with 24 nodes
    - Map nodes should appear from backend data (same positions as before)

    **Criterion 2 — Real-time node status updates:**
    - In a terminal: `curl -s http://localhost:8000/api/topology | python -m json.tool | head -20`
    - Watch the map — node colors should occasionally flicker as sensor noise crosses thresholds
    - Optionally open Network tab → WS tab → confirm WebSocket frames arriving every ~1 second

    **Criterion 3 — Live sidebar charts:**
    - Click any node on the map
    - Sidebar should show "Live Charts" section with two sparklines (blue voltage, green frequency)
    - Wait 15 seconds — both charts should grow rightward as readings accumulate

    **Criterion 4 — Storm simulation:**
    - Click "Simulate Storm Event" in the sidebar
    - Button should show "Injecting..." then "Storm Injected"
    - Within 2-3 seconds: 4-5 node dots on the map should change to red/yellow
    - Node detail panel (if a node is selected) should show updated status badge

    **Criterion 5 — WebSocket reconnection:**
    - Stop the backend (Ctrl+C in backend terminal)
    - Header should switch to yellow "RECONNECTING" indicator
    - Restart the backend (`python -m backend.main`)
    - Within 1-30 seconds (depending on backoff position): header returns to green LIVE
    - No manual page refresh required
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 criteria pass, or describe which criteria failed and what was observed</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 2 roadmap success criteria confirmed by human review:
1. Topology loads from FastAPI REST on page load
2. Node status colors update in real time via WebSocket
3. Sidebar shows live voltage/frequency charts for selected node
4. Storm event visibly changes node states within a few seconds
5. WebSocket reconnects automatically after backend restart
</verification>

<success_criteria>
- All 5 automated pre-checks pass
- Human reviewer confirms all 5 Phase 2 success criteria
- Phase 2 marked complete in ROADMAP.md progress table
- STATE.md updated with current position (Phase 3 next)
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-and-data-pipeline/02-06-SUMMARY.md`

Update ROADMAP.md Phase 2 progress row: `| 2. Backend & Data Pipeline | 6/6 | Complete | {date} |`
Update STATE.md: Current Phase = 3, Status = ready for Phase 3 planning
</output>
