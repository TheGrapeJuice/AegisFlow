---
phase: 03-xgboost-anomaly-detection
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified: []
autonomous: false
requirements:
  - ML-01
  - UX-03

must_haves:
  truths:
    - "Storm event causes XGBoost model (not a hardcoded rule) to flag affected nodes"
    - "Anomaly alert panel auto-opens showing timestamped alerts with numeric anomaly scores"
    - "Alerts can be individually dismissed from the panel"
    - "Inference loop runs asynchronously — no visible lag spike on storm trigger"
  artifacts: []
  key_links:
    - from: "Anomaly panel"
      to: "WebSocket node data"
      via: "is_anomalous field in live node updates drives alert upsert in DashboardLayout"
      pattern: "is_anomalous"
---

<objective>
Human verification of all 4 Phase 3 success criteria. Confirms the full ML pipeline end-to-end: storm trigger → XGBoost detection → anomaly panel UI.

Purpose: Validates that the system moved from rule-based thresholds to a real ML model, and that the alert UX works as designed.
Output: Human approval or list of issues for gap closure.
</objective>

<execution_context>
@C:/Users/saads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/saads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-xgboost-anomaly-detection/03-03-SUMMARY.md
@.planning/phases/03-xgboost-anomaly-detection/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification automated checks</name>
  <files></files>
  <action>
Run the following automated checks and report results before asking for human verification:

1. npm run build exits 0 (no TypeScript errors):
   cd "C:/Users/saads/Documents/Aegis Flow" && npm run build

2. Backend imports cleanly with no errors:
   python -c "from backend.main import app; print('backend import OK')"

3. Model artifact exists and is non-empty:
   python -c "import os; size = os.path.getsize('backend/ml/model.json'); assert size > 1000, f'model.json too small: {size} bytes'; print(f'model.json OK — {size} bytes')"

4. Inference module loads and runs against NODE_STATES:
   python -c "
from backend.ml.inference import predict_anomalies
from backend.state import NODE_STATES
results = predict_anomalies(NODE_STATES)
assert len(results) == 24
print(f'inference OK — {len(results)} nodes, {sum(1 for s,f in results.values() if f)} flagged')
"

5. AnomalyPanel exists in component tree:
   grep -r "AnomalyPanel" "C:/Users/saads/Documents/Aegis Flow/src/components/layout/DashboardLayout.tsx" && echo "AnomalyPanel wired"

6. GridNode has anomaly fields:
   grep "anomaly_score" "C:/Users/saads/Documents/Aegis Flow/src/types/grid.ts" && echo "GridNode types OK"

Report pass/fail for each check. If any check fails, fix the issue before presenting for human review.
  </action>
  <verify>
    <automated>cd "C:/Users/saads/Documents/Aegis Flow" && npm run build 2>&1 | tail -3 && python -c "from backend.main import app; print('backend OK')"</automated>
  </verify>
  <done>All 6 automated checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all 4 Phase 3 success criteria</name>
  <what-built>
Full Phase 3 ML pipeline:
- XGBoost model trained on synthetic sensor data and committed as backend/ml/model.json
- Inference loop running as async background task every 5 seconds, populating anomaly_score and is_anomalous on each node
- WebSocket payload extended with anomaly fields — no frontend reconnect needed
- AnomalyPanel overlay auto-opens when first anomalous node detected, shows node ID + numeric score + timestamp
- Per-row dismiss removes alert; dismissed node re-alerts after next storm event
  </what-built>
  <how-to-verify>
Start both backend and frontend:
  Terminal 1: cd "C:/Users/saads/Documents/Aegis Flow" && uvicorn backend.main:app --reload
  Terminal 2: cd "C:/Users/saads/Documents/Aegis Flow" && npm run dev

Then verify all 4 Phase 3 success criteria:

1. ANOMALY DETECTION (not hardcoded rules):
   - Open http://localhost:5173
   - Wait 5-10 seconds for inference to warm up
   - Click "Simulate Storm Event"
   - Within 5-10 seconds, affected node(s) should turn red AND an anomaly panel should slide in from the right
   - Confirm the panel is appearing due to ML inference (score values like 0.73, 0.89 — not binary 1.0)

2. ANOMALY ALERT PANEL:
   - Confirm the panel shows: node identifier (e.g. "NODE-07"), a numeric anomaly score (e.g. "0.87"), and a timestamp
   - Confirm the panel auto-opened without any manual toggle

3. DISMISS BEHAVIOR:
   - Click the X button on one alert row
   - Confirm only that row disappears; other rows remain
   - Trigger a second storm event
   - Confirm the previously dismissed node can get a fresh alert row

4. NO LAG:
   - While storm is active, watch the Event Log in the right panel
   - Confirm node state transitions continue updating every ~1 second (no visible freeze or lag spike)
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 criteria pass, or describe which criteria failed</resume-signal>
  <action>Start backend and frontend, then manually verify all 4 Phase 3 success criteria as described in how-to-verify.</action>
  <verify>Human types "approved" after confirming all 4 criteria.</verify>
  <done>Human reviewer confirms all 4 Phase 3 success criteria are met and types "approved".</done>
</task>

</tasks>

<verification>
All 4 Phase 3 roadmap success criteria confirmed by human reviewer:
1. XGBoost flags anomalies (non-hardcoded, shows numeric scores)
2. Anomaly panel shows timestamped alerts with node ID and score
3. Per-row dismiss works; dismissed nodes re-alert after next storm
4. No lag spike on storm trigger (WebSocket updates continue at 1s cadence)
</verification>

<success_criteria>
Human reviewer types "approved" confirming all 4 Phase 3 success criteria are met.
</success_criteria>

<output>
After completion, create .planning/phases/03-xgboost-anomaly-detection/03-04-SUMMARY.md
Update ROADMAP.md: Phase 3 status → Complete, completion date → today
Update STATE.md: Phase → 4, Current Plan → 0, Status → Ready for Phase 4 planning
</output>
