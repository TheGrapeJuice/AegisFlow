---
phase: 04-gnn-cascade-failure-prediction
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified: []
autonomous: false
requirements: [ML-02, ML-03, ML-04, MAP-04, UX-04]

must_haves:
  truths:
    - "Cascade nodes turn amber (orange/amber) with timing labels and confidence badges on the map after storm trigger"
    - "Blue rerouting path appears after cascade animation"
    - "CascadePanel in sidebar shows ranked at-risk nodes with confidence scores and rerouting summary"
    - "Cascade visualization only triggers on anomaly state change, not every tick"
  artifacts:
    - path: "backend/ml/gnn_model.pt"
      provides: "Trained GCN model artifact"
    - path: "backend/ml/gnn_inference.py"
      provides: "predict_cascade() function"
    - path: "src/components/layout/CascadePanel.tsx"
      provides: "Cascade risk sidebar panel"
  key_links:
    - from: "storm trigger button"
      to: "amber cascade nodes on map"
      via: "XGBoost anomaly → GNN cascade inference → frontend visualization"
      pattern: "cascade"
---

<objective>
Human verification of all 4 Phase 4 success criteria from ROADMAP.md.

Purpose: Phase 4 is a visual + ML feature — automated tests cannot confirm the map looks right, the animation sequence is correct, or the narrative flow is compelling. Human eyes required.

Output: Phase 4 approved or issues logged for gap closure.
</objective>

<execution_context>
@C:/Users/saads/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/saads/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of all 4 Phase 4 success criteria</name>
  <action>Verify cascade failure prediction pipeline end-to-end: amber cascade nodes on map with timing labels and confidence badges, blue rerouting overlay, CascadePanel sidebar panel. See how-to-verify for exact steps.</action>
  <what-built>
Phase 4 complete: GNN cascade failure prediction pipeline, cascade map visualization (amber pulse + timing + confidence), blue rerouting overlay, CascadePanel sidebar, 30s auto-fade.

Backend running: `python -m backend.main`
Frontend running: `npm run dev` → http://localhost:5173
  </what-built>
  <how-to-verify>
Follow this verification sequence:

**Setup:**
1. Start backend: `python -m backend.main`
2. Start frontend: `npm run dev`
3. Open http://localhost:5173

**Criterion 1: Cascade chain highlights on map with timing labels**
4. Click "Simulate Storm Event" in the sidebar
5. Wait 5-10 seconds for XGBoost anomaly detection (some nodes turn red)
6. Wait another 5 seconds for GNN cascade prediction
7. VERIFY: Some nodes turn AMBER/ORANGE (not red) — these are the predicted cascade nodes
8. VERIFY: Floating text labels appear ABOVE each amber node (e.g., "~4 min", "~7 min")
9. PASS: At least 2 cascade nodes are highlighted on the map with timing labels

**Criterion 2: Confidence scores displayed**
10. VERIFY: Each amber node shows a percentage badge BELOW its name label (e.g., "87% risk")
11. VERIFY: High-confidence nodes (>75%) appear brighter and pulsing; lower-confidence nodes appear steadier/fainter
12. PASS: Confidence percentages visible on the map for cascade nodes

**Criterion 3: Blue rerouting path appears**
13. VERIFY: A BLUE DASHED LINE appears connecting a path of nodes on the map — this is the rerouting recommendation
14. VERIFY: The blue line appears roughly 1-2 seconds AFTER the amber cascade nodes (sequential reveal)
15. PASS: Blue dashed rerouting line is visible on the map overlay

**Criterion 4: CascadePanel in sidebar**
16. VERIFY: Right sidebar shows a "CASCADE RISK" section (amber header)
17. VERIFY: Panel shows a ranked list of at-risk nodes with: node ID, confidence %, timing estimate
18. VERIFY: Panel shows a rerouting summary text (e.g., "Rerouting: via ... +12% margin") in blue
19. VERIFY: When no storm is active, panel shows "No cascade risk detected"
20. PASS: CascadePanel functions correctly

**Criterion 4b: No animation flicker on every tick**
21. Watch the dashboard with no storm active for 5 seconds
22. VERIFY: Amber nodes and timing labels do NOT appear during normal operation
23. VERIFY: Cascade visualization only appears after storm trigger and anomaly detection
24. PASS: No phantom cascade flicker on idle state

**Criterion 4c: 30-second auto-fade**
25. (Optional) Wait 30 seconds after cascade appears — VERIFY: cascade gradually disappears without interaction
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 criteria pass, or describe any issues seen (which step failed, what was observed vs expected)</resume-signal>
</task>

</tasks>

<verification>
Phase 4 approved when all ROADMAP criteria pass:
1. XGBoost anomaly → cascade chain highlights in amber on map with timing labels
2. Each cascade node shows confidence score (e.g., "87% cascade risk")
3. Blue rerouting path highlights after cascade animation
4. Cascade only triggers on state change, not every WebSocket tick
</verification>

<success_criteria>
User types "approved" after confirming all 4 Phase 4 ROADMAP success criteria pass:
  1. Nodes in predicted cascade chain highlight amber with timing labels (e.g., "~4 min")
  2. Each at-risk node displays confidence score (e.g., "87% cascade risk")
  3. Recommended rerouting path highlights in blue on map overlay
  4. Cascade visualization only triggers on state change (no flicker)
</success_criteria>

<output>
After completion, create `.planning/phases/04-gnn-cascade-failure-prediction/04-05-SUMMARY.md`
</output>
